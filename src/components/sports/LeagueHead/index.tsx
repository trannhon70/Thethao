import { LeagueItemProps } from "@/interface";

const LeagueHead = ({ league }: { league: LeagueItemProps }) => {
  return (
    <div className="container">
      <div className="wrap-block-league">asdasds</div>
    </div>
  );
};

export default LeagueHead;
